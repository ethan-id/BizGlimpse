export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "Next.js + NextUI App with Flask API for scraping Yahoo Finance",
	description: "lessgo",
	navItems: [
		{
			label: "Home",
			href: "/",
		}
	]
};
